package example.kozaczekapp;

import android.app.Activity;
import android.os.Bundle;
import android.os.PersistableBundle;

public class SignUpActivity extends Activity {


    @Override
    public void onCreate(Bundle savedInstanceState, PersistableBundle persistentState) {
        super.onCreate(savedInstanceState, persistentState);


        //remove notification if exists

        //formularz do wypelnienia

        //ona klik -> pobranie tokenu + zalozenie konta na urzadzeniu z tokenem
        // jak sie udało -> otworz main activity + finish() lub clear stack flag
        // dopoki nie mamy tokenu, nie wracamy do main acivity

        //progress rejestracji

        //jak nie to jakies info do uzytkownika o bledzie
    }
}
